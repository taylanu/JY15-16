package proj111;

import java.util.Scanner;

public class proj111 {

	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);
		System.out.println("Type in as many words as you'd like=");
		int wordCount, sentenceLen, startPos, endPos == 0;
		System.out.println(wordCount);
		/*
		 * 		
		 * String strIn = input.next();
		for(int i = 0; i <//number of words){
			//Builds that many new strings
			}
		 */
	}
}

/*
	String x
	String y
	for (int i = 0; i<x.length();i++){
				if(strIn.charAt(i) == ' ')
					break;
				y+=charAt(i);
				
	//Used to search and build words within a sentence.
*/